# Server
PORT=3000
NODE_ENV=development

# Session - use a long random string in production
SESSION_SECRET=your-secret-key-change-in-production

# Cookie - set to true when using HTTPS
COOKIE_SECURE=false

# Optional: backup directory for backup script (relative to project root)
BACKUP_DIR=backups
